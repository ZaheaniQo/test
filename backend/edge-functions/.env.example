# Environment variables for Supabase Edge Functions
# These should be set as secrets in your Supabase project dashboard
# or using the Supabase CLI: `supabase secrets set ...`

# Your project's Supabase URL
SUPABASE_URL="https://your-project-ref.supabase.co"

# Your project's service role key, for admin-level access
SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key"

# Your project's anon key, if needed for any client-side logic within functions
SUPABASE_ANON_KEY="your-anon-key"

# Firebase Cloud Messaging (FCM) Server Key for sending push notifications
FCM_SERVER_KEY="your-fcm-server-key"
