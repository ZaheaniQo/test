# Supabase environment variables for the Next.js app
# These are safe to be exposed to the client.
NEXT_PUBLIC_SUPABASE_URL="https://your-project-ref.supabase.co"
NEXT_PUBLIC_SUPABASE_ANON_KEY="your-anon-key"

# Google Maps API Key for the web dashboard
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="your-google-maps-web-api-key"

# --- SERVER-ONLY VARIABLES ---
# These should be set in your hosting environment (e.g., Vercel, Netlify)
# and should NOT be prefixed with NEXT_PUBLIC_
# Use these for server-side actions or API routes.
SUPABASE_SERVICE_ROLE_KEY="your-supabase-service-role-key"
